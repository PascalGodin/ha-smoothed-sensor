{
  "config": {
    "step": {
      "user": {
        "title": "Create a Smoothed Sensor (EMA)",
        "description": "Aggregates the source within each update interval, then applies exponential smoothing (EMA) with your chosen decay time. Creates one output sensor, and optionally exposes the raw input as a separate sensor.",
        "data": {
          "name": "Name",
          "source": "Source sensor",
          "interval": "Update interval (seconds)",
          "decay_time_min": "Decay time τ (minutes)",
          "unit": "Override unit (optional)",
          "aggregation": "Aggregation per interval",
          "expose_input": "Expose input as separate sensor"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Edit Smoothed Sensor",
        "data": {
          "name": "Name",
          "source": "Source sensor",
          "interval": "Update interval (seconds)",
          "decay_time_min": "Decay time τ (minutes)",
          "unit": "Override unit (optional)",
          "aggregation": "Aggregation per interval",
          "expose_input": "Expose input as separate sensor"
        }
      }
    }
  }
}
